// Gerado automaticamente por scripts/build-image-manifest.mjs — não editar manualmente.
export interface ManifestImage {
  src: string;
  addedAt: number;
}

export const imageManifest: ManifestImage[] = [];
